pipeline {
    agent any

    stages {
        stage('paramethers') {
            steps {
                sh "echo $tipoBranch" 
                sh "echo $nomeBranch" 
            }
        }
        stage('create-branch') { 
            steps {
                withCredentials([usernamePassword(credentialsId: 'adeganutti-github-token')]){
                    sh "git checkout -b $tipoBranch/$nomeBranch"
                    sh "git push https://github.com/adeganutti/spring-petclinic-github.git"      
                }
            }
        }
    }
}
